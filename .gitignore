# IDE
# Note: .idea and .iml files were previously ignored. You requested these to be tracked,
# so the most common ignore patterns for IDE files were removed.
.vscode/
*.swp
*.swo

# Build
# Note: `out/` is no longer ignored to allow pushing build output if you want.
target/
bin/
*.class
*.jar

# Dependencies
# Note: `lib/` is no longer ignored so local libraries can be pushed.

# Configuration files with sensitive data
config.properties
*.env
*.local
credentials.txt

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Compiled output
*.com
*.exe
